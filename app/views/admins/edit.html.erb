<%= form_for(@admin) do |f| %>
		<%= f.label "Admin's Name" %>
		<%= f.text_field :name %>
	
		<%= f.label "Admin's Email Address" %>
		<%= f.text_field :email %>
		
	<%= f.submit "Update Admin", class: 'button' %>
<% end %>