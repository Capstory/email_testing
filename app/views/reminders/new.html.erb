<%= form_for(@reminder) do |f| %>
	<%= f.label :email %>
	<%= f.text_field :email %>
	
	<%= f.label :date, "When should we email you?" %>
	<%= f.text_field :date, type: 'date' %>
	
	<%= f.submit "Submit", class: "button" %>
<% end %>