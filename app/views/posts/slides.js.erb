<% unless @slides.empty? %>
  <% @slides.each do |url| %>
    capsule_slideshow.add_new_element("<%= url %>");
  <% end %>
  capsule_slideshow.change_data_id("<%= @id %>");
<% end %>

SlideshowPoller.poll()
